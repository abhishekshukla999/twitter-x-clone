function Feed({ children }) {
    return (
        <section className="xl:flex-[0_0_46%] lg:flex-[0_0_50%]  md:flex-[0_0_70%] 200 w-full max-w-full">
            {children}
        </section>
    );
}

export default Feed;
